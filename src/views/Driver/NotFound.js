import React from 'react'
import PropTypes from 'prop-types'
import NotFound from '../../components/Driver/NotFound';

const notFound = props => {
    return (
        <div>
            <NotFound />
        </div>
    )
}

notFound.propTypes = {

}

export default notFound
